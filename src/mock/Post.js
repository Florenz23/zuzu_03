class Post {
  constructor(id,message,rating,distance,replies,bgColor) {
    this.id = id,
    this.message = message,
    this.rating = rating,
    this.distance = distance,
    this.replies = replies,
    this.bgColor = bgColor
  }
}

export default Post
